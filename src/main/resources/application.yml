spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/workflow?characterEncoding=utf8&useSSL=false&useJDBCCompliantTimezoneShift=true&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
camunda:
  bpm:
    database:
      # 每次启动删除表再建表
      schema-update: drop-create